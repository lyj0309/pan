name: deploy azure-web-app

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: '1 15 * * *'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
        run: |
          n=`date -d "1 day ago" "+%Y-%m-%d"`